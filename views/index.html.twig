<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Raleway" rel="stylesheet">
    <link href="css/styles.css" rel="stylesheet" type="text/css">
    <title>Curl up and Dye Salon</title>
</head>
<body>
    <h1><img src="/girl.png">Our Stylists</h1>
    <div class="container">
        <div class="row">
            <div class="col-md-6">
                <h2>Add a new Stylist: </h2>
                <form action="/addStylist" method="post">
                    <input class="form-control" type="text" name="stylist" placeholder="New Stylist" required>
                    <br>
                    <button type="submit" class="btn">Add a Stylist</button>
                </form>
            </div>
            <div class="col-md-6">
                {% if stylist is not empty %}
                    <h2>Click to add or see more:</h2>
                    {% for stylist in stylist %}
                    <h4><a href="updateStylist{{ stylist.getId }}">{{ stylist.getName }}</a> </h4>
                    {% endfor %}
                    <br>
                    <form action="/deleteAllStylist" method="post">
                        <button type="submit" class="btn">Delete ALL Stylists</button>
                    </form>

                {% endif %}
            </div>
        </div>
    </div>
</body>
</html>
